# 統計ダッシュボード 詳細設計書

> **最終更新**: 2025年12月18日
>
> 本ドキュメントは、スタッフ・家族が共有する統計ダッシュボードの詳細設計を定義します。

## 実装ステータス

| 機能 | Phase | ステータス | 備考 |
|------|-------|-----------|------|
| 品物状況タブ | 8.3 | ✅ 実装完了 | セクション3 |
| 摂食傾向タブ | 8.3 | ✅ 実装完了 | セクション4 |
| AI摂食分析コンポーネント | 8.4.1 | ✅ 実装完了 | セクション4.3 |
| アラートタブ | 8.3 | 📋 設計のみ | セクション5 |
| レポートタブ | - | 📋 設計のみ | セクション6 |
| CSV出力 | - | 📋 設計のみ | セクション6.2 |

---

## 1. 概要

### 1.1 目的

品物管理・食事記録のデータを統計化し、以下を実現します：

1. **状況の見える化**: 在庫状況、摂食傾向をグラフで可視化
2. **認識共有**: スタッフと家族が同じ統計を見て状況を共有
3. **問題の早期発見**: 摂食率低下、期限切れリスクを早期に把握
4. **ケア改善**: データに基づくケアの改善提案

### 1.2 設計原則

1. **共通ビュー**: スタッフ・家族は同じ統計画面を閲覧
2. **軽量実装**: 外部グラフライブラリを使わずSVGベースで描画
3. **リアルタイム性**: データ更新時に自動で統計を再計算
4. **期間指定**: 週・月・年単位で期間を切り替え可能

---

## 2. 統計タブ構成

```
┌─────────────────────────────────────────────┐
│ [品物状況] [摂食傾向] [アラート] [レポート] │
└─────────────────────────────────────────────┘
```

| タブ | 内容 |
|------|------|
| **品物状況** | 在庫推移、カテゴリ別分布、賞味期限カレンダー |
| **摂食傾向** | 摂食率推移、品目別ランキング、主食/副食傾向 |
| **アラート** | 賞味期限警告、在庫切れ、摂食率低下 |
| **レポート** | AI生成レポート、CSV出力 |

---

## 3. 品物状況タブ

### 3.1 表示内容

#### 在庫推移グラフ（折れ線）

```
┌─────────────────────────────────────────────┐
│ 📦 在庫推移（過去7日間）                    │
├─────────────────────────────────────────────┤
│                                             │
│  15 ┤                    ●                  │
│     │                   /                   │
│  10 ┤        ●────●────●                    │
│     │       /                               │
│   5 ┤  ●───●                                │
│     │                                       │
│   0 ├──┬──┬──┬──┬──┬──┬──                  │
│     12/10 11 12 13 14 15 16                 │
│                                             │
│  ── 果物  ── お菓子  ── 飲み物              │
└─────────────────────────────────────────────┘
```

#### カテゴリ別分布（円グラフ）

```
┌─────────────────────────────────────────────┐
│ 📊 カテゴリ別分布                           │
├─────────────────────────────────────────────┤
│                                             │
│         ┌───────┐                           │
│        /  果物   \                          │
│       │   40%    │                          │
│      ┌┴──────────┴┐                         │
│      │ お菓子 30% │                         │
│      └┬──────────┬┘                         │
│       │ 飲み物   │                          │
│        \  20%   /                           │
│         └──10%─┘ その他                     │
│                                             │
│  果物: 8個  お菓子: 6個  飲み物: 4個        │
└─────────────────────────────────────────────┘
```

#### 賞味期限カレンダー

```
┌─────────────────────────────────────────────┐
│ 📅 賞味期限カレンダー          2025年12月   │
├─────────────────────────────────────────────┤
│  日  月  火  水  木  金  土                  │
│                          14                 │
│  15  16  17  18  19  20  21                 │
│      🔴  🟡                                  │
│       2   1                                 │
│  22  23  24  25  26  27  28                 │
│          🟡                                  │
│           1                                 │
├─────────────────────────────────────────────┤
│ 🔴 本日期限: 2件  🟡 3日以内: 2件           │
└─────────────────────────────────────────────┘
```

### 3.2 データ構造

```typescript
interface ItemStatsData {
  // 在庫推移
  stockTrend: {
    date: string;
    fruit: number;
    snack: number;
    drink: number;
    dairy: number;
    other: number;
    total: number;
  }[];

  // カテゴリ別分布
  categoryDistribution: {
    category: ItemCategory;
    count: number;
    percentage: number;
  }[];

  // 賞味期限
  expirationCalendar: {
    date: string;
    items: {
      id: string;
      itemName: string;
      daysUntil: number;
    }[];
  }[];

  // サマリ
  summary: {
    totalItems: number;
    pendingItems: number;
    expiringToday: number;
    expiringIn3Days: number;
  };
}
```

---

## 4. 摂食傾向タブ

### 4.1 表示内容

#### 摂食率推移グラフ（折れ線）

```
┌─────────────────────────────────────────────┐
│ 🍽️ 摂食率推移（過去7日間）                  │
├─────────────────────────────────────────────┤
│                                             │
│ 100%┤     ●────●                            │
│     │    /      \                           │
│  80%┤   ●        ●────●                     │
│     │  /              \                     │
│  60%┤ ●                ●                    │
│     │                                       │
│  40%┼──┬──┬──┬──┬──┬──┬──                  │
│     12/10 11 12 13 14 15 16                 │
│                                             │
│  平均摂食率: 78%  前週比: +5%              │
└─────────────────────────────────────────────┘
```

#### よく食べる品目ランキング

```
┌─────────────────────────────────────────────┐
│ 😋 よく食べる品目 TOP5                      │
├─────────────────────────────────────────────┤
│                                             │
│ 1. プリン        ████████████████████  95%  │
│ 2. ヨーグルト    ███████████████████   90%  │
│ 3. バナナ        ██████████████████    85%  │
│ 4. みかん        █████████████████     80%  │
│ 5. クッキー      ████████████████      75%  │
│                                             │
└─────────────────────────────────────────────┘
```

#### よく残す品目ランキング

```
┌─────────────────────────────────────────────┐
│ 😔 よく残す品目 TOP5                        │
├─────────────────────────────────────────────┤
│                                             │
│ 1. リンゴ        ████                  20%  │
│ 2. せんべい      █████                 25%  │
│ 3. キウイ        ██████                30%  │
│ 4. ゼリー        ███████               35%  │
│ 5. ケーキ        ████████              40%  │
│                                             │
│ ⚠️ リンゴは固さが原因かもしれません         │
│    → 提供方法を「すりおろし」に変更検討    │
└─────────────────────────────────────────────┘
```

#### 主食/副食摂取傾向（既存記録から）

```
┌─────────────────────────────────────────────┐
│ 🍚 食事摂取傾向（過去30日間）               │
├─────────────────────────────────────────────┤
│                                             │
│ 主食                                        │
│ ┌─────────────────────────────────────────┐ │
│ │ 8割以上 ████████████████████████  72%  │ │
│ │ 5-7割   ████████                  20%  │ │
│ │ 5割未満 ███                        8%  │ │
│ └─────────────────────────────────────────┘ │
│                                             │
│ 副食                                        │
│ ┌─────────────────────────────────────────┐ │
│ │ 8割以上 ██████████████████████    65%  │ │
│ │ 5-7割   ████████████              25%  │ │
│ │ 5割未満 █████                     10%  │ │
│ └─────────────────────────────────────────┘ │
│                                             │
└─────────────────────────────────────────────┘
```

### 4.2 データ構造

```typescript
interface ConsumptionStatsData {
  // 摂食率推移
  consumptionTrend: {
    date: string;
    averageRate: number;
    recordCount: number;
  }[];

  // 品目別ランキング
  itemRanking: {
    topItems: {
      itemName: string;
      averageRate: number;
      count: number;
    }[];
    bottomItems: {
      itemName: string;
      averageRate: number;
      count: number;
      suggestion?: string;  // AI提案
    }[];
  };

  // 食事摂取傾向（既存記録から）
  mealTrend: {
    mainDish: {
      high: number;    // 8割以上の割合
      medium: number;  // 5-7割の割合
      low: number;     // 5割未満の割合
    };
    sideDish: {
      high: number;
      medium: number;
      low: number;
    };
  };

  // サマリ
  summary: {
    averageRate: number;
    weeklyChange: number;  // 前週比
    totalRecords: number;
  };
}
```

### 4.3 AI摂食分析コンポーネント（Phase 8.4.1）

摂食傾向タブには、Gemini AI を活用した分析コンポーネントが統合されています。

> **詳細設計**: [AI_INTEGRATION_SPEC.md](./AI_INTEGRATION_SPEC.md) セクション3.2 を参照

#### 表示場所

```
┌─────────────────────────────────────────────┐
│ 🍽️ 摂食傾向                                 │
├─────────────────────────────────────────────┤
│                                             │
│  [摂食率推移グラフ]                         │
│  [品目別ランキング]                         │
│                                             │
│ ┌─────────────────────────────────────────┐ │
│ │ 🤖 AI分析                     [分析を開始]│ │
│ │                                         │ │
│ │ 📊 「分析を開始」ボタンをクリックすると │ │
│ │    AIが摂食傾向を分析します             │ │
│ └─────────────────────────────────────────┘ │
│                                             │
└─────────────────────────────────────────────┘
```

#### 分析実行後の表示

```
┌─────────────────────────────────────────────┐
│ 🤖 AI分析                         [再分析]  │
├─────────────────────────────────────────────┤
│                                             │
│ 過去30日間の摂食傾向を分析しました。        │
│ 全体的に果物類の摂取率が高く...            │
│                                             │
│ ── 📋 発見事項 ────────────────────────── │
│                                             │
│ ┌─────────────────────────────────────────┐ │
│ │ ✅ 果物類の摂取率向上                   │ │
│ │    前月比で15%向上しています            │ │
│ │    現在: 85%  前回: 70%  +15%          │ │
│ └─────────────────────────────────────────┘ │
│                                             │
│ ┌─────────────────────────────────────────┐ │
│ │ ⚠️ 乳製品の摂取率低下                   │ │
│ │    ヨーグルトの残量が増加傾向です       │ │
│ │    現在: 45%  前回: 60%  -15%          │ │
│ └─────────────────────────────────────────┘ │
│                                             │
│ ── 💡 改善提案 ────────────────────────── │
│                                             │
│ ┌─────────────────────────────────────────┐ │
│ │ 🔴 乳製品の見直し                       │ │
│ │    ヨーグルトの種類を変更してみること... │ │
│ │    関連品目: ヨーグルト                 │ │
│ └─────────────────────────────────────────┘ │
│                                             │
└─────────────────────────────────────────────┘
```

#### 実装ファイル

| ファイル | 説明 |
|----------|------|
| `frontend/src/components/family/AIAnalysis.tsx` | AI分析UIコンポーネント |
| `frontend/src/components/stats/ConsumptionStatsTab.tsx` | 摂食傾向タブ（AIAnalysisを内包） |
| `frontend/src/api/index.ts` | `aiAnalyze()` API呼び出し関数 |
| `functions/src/functions/aiAnalyze.ts` | Cloud Functions エンドポイント |

#### API連携

```typescript
// API呼び出し
const response = await aiAnalyze({
  residentId: 'R001',
  analysisType: 'consumption',
  period: {
    startDate: '2025-11-18',
    endDate: '2025-12-18'
  }
});

// レスポンス例
{
  success: true,
  data: {
    summary: "過去30日間の摂食傾向を分析しました...",
    findings: [...],
    suggestions: [...],
    analyzedAt: "2025-12-18T12:00:00.000Z"
  }
}
```

#### デモモード対応

デモモード（`/demo/stats`）では、`DEMO_FOOD_STATS` を使用してローカルデータを表示。
AI分析ボタンは実際のAPIを呼び出し、モックデータがない場合は警告付きで概要のみ返却。

---

## 5. アラートタブ

### 5.1 表示内容

```
┌─────────────────────────────────────────────┐
│ 🔔 アラート                                 │
├─────────────────────────────────────────────┤
│                                             │
│ ── 🔴 緊急 (2) ─────────────────────────── │
│                                             │
│ ┌─────────────────────────────────────────┐ │
│ │ ⏰ キウイの賞味期限が本日です           │ │
│ │    期限: 12/16  残: 3個                 │ │
│ │    [品物を見る]                          │ │
│ └─────────────────────────────────────────┘ │
│                                             │
│ ┌─────────────────────────────────────────┐ │
│ │ 📉 リンゴの摂食率が低下しています       │ │
│ │    直近7日: 20%（前週: 60%）            │ │
│ │    [傾向を見る]                          │ │
│ └─────────────────────────────────────────┘ │
│                                             │
│ ── 🟠 注意 (3) ─────────────────────────── │
│                                             │
│ ┌─────────────────────────────────────────┐ │
│ │ ⏰ プリンの賞味期限が3日以内です        │ │
│ │    期限: 12/19  残: 2個                 │ │
│ │    [品物を見る]                          │ │
│ └─────────────────────────────────────────┘ │
│                                             │
│ ┌─────────────────────────────────────────┐ │
│ │ 📦 ヨーグルトの在庫が残り1個です        │ │
│ │    [補充を依頼]                          │ │
│ └─────────────────────────────────────────┘ │
│                                             │
└─────────────────────────────────────────────┘
```

### 5.2 アラート種別

```typescript
type AlertType =
  | 'expiration_today'      // 本日期限
  | 'expiration_soon'       // 3日以内期限
  | 'low_stock'             // 在庫少
  | 'out_of_stock'          // 在庫切れ
  | 'consumption_decline'   // 摂食率低下
  | 'no_recent_record';     // 記録なし

interface Alert {
  id: string;
  type: AlertType;
  severity: 'urgent' | 'warning' | 'info';
  title: string;
  description: string;
  relatedItemId?: string;
  createdAt: Timestamp;
}
```

---

## 6. レポートタブ

### 6.1 表示内容

```
┌─────────────────────────────────────────────┐
│ 📄 レポート                                 │
├─────────────────────────────────────────────┤
│                                             │
│ ┌─ AIサマリ ─────────────────────────────┐  │
│ │                                        │  │
│ │ 📊 今週のサマリ（12/10-16）            │  │
│ │                                        │  │
│ │ ・摂食率は平均78%で、先週より5%改善   │  │
│ │ ・果物の摂食率が高く、特にバナナが好評 │  │
│ │ ・リンゴは摂食率が低下傾向            │  │
│ │   → すりおろしなど提供方法の変更を検討│  │
│ │ ・キウイの賞味期限が本日、要対応      │  │
│ │                                        │  │
│ │ [詳細レポートを生成]                   │  │
│ └─────────────────────────────────────────┘  │
│                                             │
│ ┌─ データ出力 ───────────────────────────┐  │
│ │                                        │  │
│ │ 期間: [2025/12/01] 〜 [2025/12/16]    │  │
│ │                                        │  │
│ │ 出力内容:                              │  │
│ │ ☑️ 品物一覧                            │  │
│ │ ☑️ 摂食記録                            │  │
│ │ ☑️ 食事記録                            │  │
│ │ ☐ タスク履歴                          │  │
│ │                                        │  │
│ │ [CSVダウンロード]                      │  │
│ │                                        │  │
│ └─────────────────────────────────────────┘  │
│                                             │
└─────────────────────────────────────────────┘
```

### 6.2 CSV出力仕様

**品物一覧CSV**:
```csv
品物ID,品物名,カテゴリ,送付日,賞味期限,個数,残数,ステータス,摂食率
ITM001,キウイ,果物,2025/12/14,2025/12/18,3,1,提供済み,80%
ITM002,プリン,お菓子,2025/12/15,2025/12/20,2,0,消費済み,95%
```

**摂食記録CSV**:
```csv
記録日,品物名,カテゴリ,摂食状況,摂食率,担当スタッフ,特記事項
2025/12/16,キウイ,果物,ほぼ完食,80%,山田太郎,おいしそうに召し上がっていました
2025/12/15,プリン,お菓子,完食,100%,田中花子,
```

---

## 7. API仕様

### 7.1 統計データ取得 API

```
GET /getStats
```

**リクエスト（クエリパラメータ）**:
```typescript
interface GetStatsRequest {
  residentId?: string;           // 入居者でフィルタ
  startDate: string;             // 開始日 (YYYY-MM-DD)
  endDate: string;               // 終了日 (YYYY-MM-DD)
  include?: ('items' | 'consumption' | 'meals' | 'alerts')[]; // 取得する統計
}
```

**レスポンス**:
```typescript
interface GetStatsResponse {
  success: boolean;
  data?: {
    period: {
      start: string;
      end: string;
    };
    itemStats?: ItemStatsData;
    consumptionStats?: ConsumptionStatsData;
    mealStats?: MealStatsData;
    alerts?: Alert[];
  };
  error?: string;
}
```

### 7.2 AIレポート生成 API

```
POST /generateReport
```

**リクエスト**:
```typescript
interface GenerateReportRequest {
  residentId: string;
  startDate: string;
  endDate: string;
  reportType: 'summary' | 'detailed';
}
```

**レスポンス**:
```typescript
interface GenerateReportResponse {
  success: boolean;
  data?: {
    report: string;              // マークダウン形式
    generatedAt: string;
  };
  error?: string;
}
```

### 7.3 CSV出力 API

```
GET /exportCsv
```

**リクエスト（クエリパラメータ）**:
```typescript
interface ExportCsvRequest {
  residentId?: string;
  startDate: string;
  endDate: string;
  dataTypes: ('items' | 'consumption' | 'meals' | 'tasks')[];
}
```

**レスポンス**: CSV ファイル（Content-Type: text/csv）

---

## 8. 実装ファイル構成

### 8.1 バックエンド

```
functions/src/
├── functions/
│   ├── getStats.ts            # 統計データ取得
│   ├── generateReport.ts      # AIレポート生成
│   └── exportCsv.ts           # CSV出力
├── services/
│   └── statsService.ts        # 統計計算ロジック
└── types/
    └── stats.ts               # 型定義
```

### 8.2 フロントエンド

```
frontend/src/
├── types/
│   ├── stats.ts               # 統計型定義
│   └── careItem.ts            # AI分析関連型（AIAnalyzeResponse等）
├── pages/
│   └── shared/
│       └── StatsDashboard.tsx # 統計ダッシュボード
├── components/
│   ├── stats/
│   │   ├── StatsTabs.tsx          # タブ切り替え
│   │   ├── ItemStatsTab.tsx       # 品物状況タブ
│   │   ├── ConsumptionStatsTab.tsx # 摂食傾向タブ（AIAnalysis統合）
│   │   ├── AlertsTab.tsx          # アラートタブ
│   │   ├── ReportTab.tsx          # レポートタブ
│   │   ├── LineChart.tsx          # 折れ線グラフ（SVG）
│   │   ├── PieChart.tsx           # 円グラフ（SVG）
│   │   ├── BarChart.tsx           # 棒グラフ（SVG）
│   │   ├── ExpirationCalendar.tsx # 賞味期限カレンダー
│   │   └── RankingList.tsx        # ランキングリスト
│   └── family/
│       └── AIAnalysis.tsx         # AI摂食分析コンポーネント ✅ Phase 8.4.1
├── hooks/
│   ├── useStats.ts            # 統計データ取得
│   ├── useAlerts.ts           # アラート取得
│   └── useReport.ts           # レポート生成
├── api/
│   └── index.ts               # API呼び出し関数（aiAnalyze含む）
└── data/
    └── demo/
        └── demoStats.ts       # デモ用統計データ（DEMO_FOOD_STATS）
```

---

## 9. SVGグラフ実装例

### 9.1 折れ線グラフ

```typescript
// frontend/src/components/stats/LineChart.tsx

interface LineChartProps {
  data: { label: string; value: number }[];
  width?: number;
  height?: number;
  color?: string;
}

function LineChart({
  data,
  width = 300,
  height = 150,
  color = '#2563EB'
}: LineChartProps) {
  const padding = 30;
  const chartWidth = width - padding * 2;
  const chartHeight = height - padding * 2;

  const maxValue = Math.max(...data.map(d => d.value));
  const minValue = Math.min(...data.map(d => d.value));
  const range = maxValue - minValue || 1;

  const points = data.map((d, i) => ({
    x: padding + (i / (data.length - 1)) * chartWidth,
    y: padding + chartHeight - ((d.value - minValue) / range) * chartHeight,
  }));

  const pathD = points
    .map((p, i) => `${i === 0 ? 'M' : 'L'} ${p.x} ${p.y}`)
    .join(' ');

  return (
    <svg width={width} height={height} className="line-chart">
      {/* グリッド線 */}
      {[0, 25, 50, 75, 100].map(pct => (
        <line
          key={pct}
          x1={padding}
          y1={padding + chartHeight * (1 - pct / 100)}
          x2={width - padding}
          y2={padding + chartHeight * (1 - pct / 100)}
          stroke="#e5e7eb"
          strokeDasharray="2,2"
        />
      ))}

      {/* 折れ線 */}
      <path d={pathD} fill="none" stroke={color} strokeWidth={2} />

      {/* データポイント */}
      {points.map((p, i) => (
        <circle key={i} cx={p.x} cy={p.y} r={4} fill={color} />
      ))}

      {/* X軸ラベル */}
      {data.map((d, i) => (
        <text
          key={i}
          x={points[i].x}
          y={height - 5}
          textAnchor="middle"
          fontSize={10}
          fill="#6b7280"
        >
          {d.label}
        </text>
      ))}
    </svg>
  );
}
```

### 9.2 横棒グラフ（ランキング用）

```typescript
// frontend/src/components/stats/BarChart.tsx

interface BarChartProps {
  data: { label: string; value: number }[];
  maxValue?: number;
  color?: string;
}

function BarChart({ data, maxValue = 100, color = '#10B981' }: BarChartProps) {
  return (
    <div className="bar-chart space-y-2">
      {data.map((d, i) => (
        <div key={i} className="flex items-center gap-2">
          <span className="w-24 text-sm truncate">{d.label}</span>
          <div className="flex-1 bg-gray-200 rounded h-4">
            <div
              className="h-full rounded"
              style={{
                width: `${(d.value / maxValue) * 100}%`,
                backgroundColor: color,
              }}
            />
          </div>
          <span className="w-12 text-sm text-right">{d.value}%</span>
        </div>
      ))}
    </div>
  );
}
```

---

## 10. 参照資料

- [USER_ROLE_SPEC.md](./USER_ROLE_SPEC.md) - ユーザーロール・権限設計
- [ITEM_MANAGEMENT_SPEC.md](./ITEM_MANAGEMENT_SPEC.md) - 品物管理詳細設計
- [TASK_MANAGEMENT_SPEC.md](./TASK_MANAGEMENT_SPEC.md) - タスク管理詳細設計
- [AI_INTEGRATION_SPEC.md](./AI_INTEGRATION_SPEC.md) - AI連携設計
